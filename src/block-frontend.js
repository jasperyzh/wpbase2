import "./block-frontend.scss";

// vuejs
/* const { createApp, defineComponent, ref } = Vue;

const VueBootstrapCard = defineComponent({
    props: ['attributes'],
    setup(props) {
        console.log(props.attributes);
        const decodedAttributes = ref(props.attributes);

        return {
            decodedAttributes
        };
    },
    template: `
<div>
<h2>{{ decodedAttributes.title }}</h2>
<p>{{ decodedAttributes.description }}</p>
</div>
`
});

createApp({
    components: {
        VueBootstrapCard
    }
}).mount('#app'); */